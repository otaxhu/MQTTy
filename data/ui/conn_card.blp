using Gtk 4.0;

template $MQTTyConnCard: $MQTTyBaseCard {
  prefix_widget: Box {
    styles [
      "indicator",
      "circular",
    ]
  };

  suffix_widget: Button {
    icon-name: "document-edit-symbolic";
    clicked => $edit_button_clicked() swapped;
  };
}
